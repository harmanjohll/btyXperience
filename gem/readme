This directory will contain the requisite assets to run an interactive using vibe coded platforms. 
Two HTML files: one presenter, one audience. Assets include pictures that will be referenced. 
Gemini was used to create the HTMLs. 
